package week_11_;

public class Notes {
/*
 * == checks addresses !!!!
 * String s1 =foo
 * string s1 = foo
 * sysout(" "+ (s1==s2)) true STRING POOLING
 * if string already created , brings the reference to it 
 * instead of creating a new object with same value
 *for when a key is not unique !!
 *Map<String,List<Student>>
 *if(!map.contains(student)){
 *	 if(!fastSurname.containsKey(surname))
 * 		fastSurname.put(surname,new ArrayList<Student>()));
 * 		
 *		fastSurname.get(surname).add(student);
 *}

 * hashSet => override hashcode
 * 
 * 
 * interface default method!!!!!siiicckkk1!!!
 * defined in interface**
 * 
 * functional interface
 * java.util.function ->an interface a task to be done
 * 
 * ====lambda XXXXX
 * Comparator<student> cmp =(s1,s2)-> s1.getName().compareTo(s2.getName());
 * Collections.sort(students,cmp); passing a function-ish object
 * params ->{ /* lots of ops }
 * 
 * 
 * 
 * Collections.sort(students, (s1,s2)->s1.getName().compareTo(s2.getName()));
 * system.out::println
 * :: so useful 
 */
}
