package week_9;

public class Notes {
/*
 * MONDAY 23/04/2018
 * 
 * list of int is not subtype of list of number
 * int is a subtype o number
 *java array store exception
 *method(Point<?> p){
 *method is taking anything
 *and compatible to work with all types
 *if made correctly
 *}
 *<? super C> -min bound
 *<? extends D> -max bound
 *provides flexibility while staying object oriented
 *
 *
 *		Person<Integer> a;
 *		Person<String> b;
		a.getClass() = b.getClass(); !!!!!!
		
		
		Collections Framework
 *		
 * Abstract data types
 * Interfaces 
 * most general
 * collection => List => ArrayList, LinkedList, 
 * 			  => Queue[Priority]
 * 			  => Set => Sorted Set
 * Map => SortedMap
 *		
 *MAP iterator by entrySet()
 *
 *static long currentTimeMillis
 *
 *	FRIDAY 27/04/2018
 *	CCLLECTION is the parent
 *	MAP is associative entry-sets
 *	list interface 
 *	remove - linear 
 * 	search - slow
 *	lastindexof - last occurence in list
 *
 * 	QUEUE
 *  peek - just to look(safer way)
 *  poll - remove~
 *  element - peek ~
 *  
 *  implemented by linked list or proirityqueue= head is smallest
 *  sorted set
 *  no duplicates
 *  Hash set have internal structure ->faster
 *  Linked hash set -> safest for collisions, managed
 *  Tree set (new Comparator<Object>(){ })
 *  
 *  ITERABLE<E>()
 *  not recommended but if you have to
 *  USE THESE: ITERATOR.REMOVE()
 *  LISTITERATOR.ADD(); => to add to lists
 *  Map exam;
 *  MAP is not a collection! but similar;
 *  unique key to a value
 *  MAP<K,V>  -- cool --
 *   put(Key k , Value v)
 *   get(Object key)
 *   remove()Object key;
 *   size
 *   isEmpty
 *   clear
 *   containsKay 1<=O(depends)<=logn
 *   containsValue => slow 
 *   getKey 
 *   getValue
 *   for(/map.Entry<k,v> e : map.entrySet()){
 *   Collection<V> values();
 *   set<K>() keySet()
 *   
 *   singleton + hashmap = global directory
 *   
 *   }
 *   ///so sick
 *  for trees and sorted collections always put comparator with initialization 
 *   in map adding a value with present key it updates the value of key , ditches the old data
 *  
 *  
 *  
 *  
 * 
 */
}
